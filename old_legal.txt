    return createPortal(
      <AnimatePresence>
        {activeModal && (
          <div 
            className="fixed inset-0 z-[200] bg-black/60 backdrop-blur-sm flex justify-center items-center p-4"
            onClick={() => setActiveModal(null)}
          >
            <m.div 
              initial={{ scale: 0.95, opacity: 0, y: 20 }}
              animate={{ scale: 1, opacity: 1, y: 0 }}
              exit={{ scale: 0.95, opacity: 0, y: 20 }}
              className="bg-[var(--bg-primary)] w-full max-w-2xl max-h-[85vh] rounded-3xl flex flex-col border border-[var(--border-color)] shadow-2xl"
              onClick={e => e.stopPropagation()}
            >
              <div className="flex items-center justify-between p-6 border-b border-[var(--border-color)]">
                <h2 className="text-2xl font-bold text-[var(--text-primary)]">
                  {activeModal === 'privacy' ? 'Informativa sulla Privacy' : 'Termini di Servizio'}
                </h2>
                <button 
                  onClick={() => setActiveModal(null)}
                  className="w-8 h-8 rounded-full bg-[var(--bg-secondary)] text-[var(--text-secondary)] flex items-center justify-center hover:bg-[var(--bg-hover)] transition-colors"
                >
                  <X size={20} />
                </button>
              </div>

              <div className="flex-1 overflow-y-auto p-6 space-y-6 text-sm leading-relaxed text-[var(--text-secondary)]">
                {activeModal === 'privacy' ? (
                  <>
                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">1. Architettura "Zero-Knowledge" (E2EE)</h3>
                      <p>
                        "I miei Pensieri" è costruita con il principio della <strong>Privacy by Design</strong>. 
                        Tutti i tuoi dati (testi, registrazioni vocali, API key) vengono crittografati in locale sul tuo dispositivo utilizzando lo standard AES-GCM a 256 bit. 
                        La chiave di cifratura è derivata univocamente dalla tua password (tramite PBKDF2). Questo significa che <strong>nessuno, nemmeno noi sviluppatori, ha la possibilità tecnica di decifrare o leggere i tuoi pensieri</strong>. Su Firestore salviamo solo stringhe incomprensibili.
                      </p>
                    </section>
                    
                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">2. Integrazione AI e Dati Sensibili</h3>
                      <p>
                        Se scegli di attivare le funzioni IA (come "Frasi Ispiratrici" o "Analisi Psicologica"), i tuoi pensieri verranno decifrati temporaneamente sul tuo dispositivo e inviati alle API di Google Gemini tramite HTTPS.
                        Google processa i dati per generare la risposta ma <strong>non li utilizza per addestrare i propri modelli pubblici</strong> (come da policy di Google Cloud Enterprise/AI Studio).
                      </p>
                      <p className="mt-2 p-3 bg-orange-500/10 border border-orange-500/20 rounded-xl text-orange-600 dark:text-orange-400">
                        <strong>Avvertenza per il traduttore gratuito:</strong> Se utilizzi l'opzione "Usa Traduttore Gratuito", la crittografia E2EE decade. I testi verranno inviati in chiaro all'API di MyMemory.
                      </p>
                    </section>

                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">3. Geolocalizzazione</h3>
                      <p>
                        Se attivi la geolocalizzazione, le coordinate GPS verranno associate ai tuoi pensieri e <strong>anch'esse verranno crittografate localmente</strong> prima del salvataggio. Non teniamo traccia degli spostamenti in tempo reale.
                      </p>
                    </section>
                  </>
                ) : (
                  <>
                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">1. Limitazione di Responsabilità per Perdita Dati</h3>
                      <p>
                        Poiché utilizziamo crittografia End-to-End senza alcun sistema di recupero chiavi, <strong>se dimentichi la tua password o la perdi, perderai irreversibilmente l'accesso a tutti i tuoi dati</strong>.
                        L'utente si assume la totale responsabilità della custodia delle proprie credenziali. Lo sviluppatore non potrà mai recuperare i tuoi pensieri.
                      </p>
                    </section>

                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">2. Analisi Psicologiche non mediche</h3>
                      <p>
                        I profili psicologici e le "connessioni interiori" generate dall'IA hanno puro scopo <strong>ludico, riflessivo e personale</strong>. 
                        Non costituiscono in alcun modo diagnosi medica, psicologica o psichiatrica. In caso di disagio psicologico, ti invitiamo a rivolgerti a un professionista sanitario qualificato.
                      </p>
                    </section>

                    <section>
                      <h3 className="font-bold text-[var(--text-primary)] text-base mb-2">3. Utilizzo API Key Personale (BYOK)</h3>
                      <p>
                        L'app ti permette di inserire la tua chiave API personale (Google Gemini). Utilizzando questa funzione ("Bring Your Own Key"), sei responsabile dei costi o dei limiti imposti da Google al tuo account.
                        La chiave verrà salvata crittografata con la tua password. L'autore dell'app non è responsabile in caso di esposizione accidentale della chiave dovuta a malware sul tuo dispositivo o password deboli.
                      </p>
                    </section>
                  </>
                )}
              </div>

              <div className="p-6 border-t border-[var(--border-color)] flex flex-col sm:flex-row gap-3">
              {activeModal === 'privacy' && (
                <button 
                  onClick={() => {
                    localStorage.setItem('privacy_accepted_v2', 'false');
                    localStorage.setItem('enableAiAnalysis', 'false');
                    localStorage.setItem('enableLocation', 'false');
                    window.dispatchEvent(new Event('storage')); // trigger updates in other components
                    setActiveModal(null);
                  }}
                  className="w-full sm:w-1/2 bg-[var(--bg-primary)] border-2 border-[var(--border-color)] text-[var(--text-primary)] py-4 rounded-xl font-bold transition-transform active:scale-95 text-base sm:text-lg hover:bg-[var(--bg-hover)]"
                >
                  Rifiuta IA e GPS
                </button>
              )}
                <button 
                  onClick={() => setActiveModal(null)}
                  className={`w-full ${activeModal === 'privacy' ? 'sm:w-1/2' : ''} bg-[var(--text-primary)] text-[var(--bg-base)] py-4 rounded-xl font-bold transition-transform active:scale-95 text-base sm:text-lg`}
                >
                  Ho capito
                </button>
              </div>
            </m.div>
          </div>
        )}
      </AnimatePresence>
    );